<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Inter", sans-serif;
        line-height: 1.6;
        color: #2c3e50;
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
        text-align: center;
      }

      .hero {
        background: rgba(255, 255, 255, 0.9);
        padding: 3rem;
        border-radius: 1rem;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        backdrop-filter: blur(10px);
        margin: 2rem;
      }

      h1 {
        font-size: 3rem;
        margin-bottom: 1.5rem;
        background: linear-gradient(135deg, #00416a 0%, #e4e5e6 100%);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        font-weight: 700;
      }

      p {
        font-size: 1.2rem;
        color: #666;
        margin-bottom: 2rem;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
      }

      .cta-button {
        display: inline-block;
        padding: 1rem 2rem;
        background: linear-gradient(135deg, #00416a 0%, #1976d2 100%);
        color: white;
        text-decoration: none;
        border-radius: 0.5rem;
        font-weight: 500;
        transition: transform 0.2s, box-shadow 0.2s;
      }

      .cta-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
      }

      @media (max-width: 768px) {
        .hero {
          padding: 2rem;
          margin: 1rem;
        }

        h1 {
          font-size: 2rem;
        }

        p {
          font-size: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="hero">
        <h1>Welcome to the LTI Tool</h1>
        <p>
          Enhance your learning experience with our powerful LTI integration.
          Seamlessly connect with Canvas to unlock advanced features and
          capabilities.
        </p>
        <a
          href="https://github.com/Cvmcosta/ltijs"
          class="cta-button"
          target="_blank"
          >LTIJS Docs</a
        >
      </div>
    </div>
    <h1><%= message %></h1>
    <p>Successfully connected to the LTI platform!</p>

    <!-- Optional: Display some token information -->
    <% if (token) { %>
    <p>User: <%= token.user %></p>
    <% } %>
  </body>
</html>
